# 1주차

# 1-1
자바스크립트 언어는 웹 페이지를 만들기 위해 태어난 사람~

자바스크립트는 정적인 html, css의 근육이다

자바스크립트 엔진에 의해 구동된다 -> 당연한 말이죠!

개발자 도구 열고 실습 -> 안할거에용
콘솔 열고 console.log

# 1-3
live server가 변경사항 있으면 알아서 새로고침 시켜줌!
우측 하단에 포트 눌러서 서버 종료할 수 있음

# 1-4
자바스크립트 기본 문법
변수와 상수는 값을 저장하는 박스

변수에 값을 담는 것을 초기화한다 라고 함 -> 초기값을 정한다

let으로 선언했으면 다시 선언은 불가능

상수는 const로 선언. 초기화한 다음에 다시 값을 지정할 수 없다.
선언과 동시에 초기화하지 않으면 오류

변수 명명할 때는 해당 변수의 기능을 알 수 있도록 정성스럽게 지어주자

# 1-5
자료형은 집합이다
원시 타입 - number string boolean null undefined
객체 타입 - object-Array, Function, RegecExp

# 1-8
` 백틱으로 변수를 초기화할 때는 $를 사용하여
동적인 변수를 지정할 수 있다 -> 템플릿 리터럴 문법

Null -> 개발자가 비워둠을 의도했고 인지하고 있음
undefined -> 개발자가 비워둠을 인지못함 깜빡함

# 1-9 형변환
어떤 값의 타입을 다른 타입으로 변경하는 것을 말함
Num -> string

묵시적 형 변환, 명시적 형 변환
묵시적은 엔진이 알아서
명시적은 개발자가 의도해서

문자열 + 숫자 -> 숫자를 문자열로 변환시킴

parseInt 쓸 때는 숫자가 앞쪽에 있어야 한다.

# 1-10 연산자
++을 뒤에 붙이면 그 라인이 끝나고 나서야 1이 더해짐
앞에 붙이면 그 라인에서 이루어짐

== -> 자료형은 비교 X
=== -> 자료형까지 비교

# 1-11 연산자2
null 병합 연산자는 언제쓰지
실무에서 요긴함
-> 회원가입 시스템에서 둘 중 있는 값을 가져올 때

# 1-12 조건문
if와 switch
if 뒤엔 조건이 온다. -> if, else if
if로 시작하고 else로 끝난다.

어떤 변수의 값을 기준으로 조건을 붙이고 싶다 -> switch
조건문을 기준으로 나누고 싶다 -> if

# 1-13 반복문

# 1-14 함수
중복된 기능의 코드를 함수로 만들어서 생산성 향상 및 유지보수에 용이

인수와 매개변수

선언이 일어나지 않은 함수를 먼저 실행시켜도 호이스팅되어
알아서 잘 실행됨
호잇

# 1-15 

함수 표현식으로 만든 함수들은 호이스팅의 대상이 되지 않는다
값으로써 기능하기 때문에

# 1-16 콜백함수
매개변수 자리에 함수를 넣을 수 있다 -> 호출시킨다
=> 매개변수 자리에 오는 함수가 바로 "콜백함수"

함수 표현식으로 만든 함수는 호이스팅이 안되는거 주의!

# 1-17 스코프

전역, 지역

# 1-18 객체1

# 1-19 객체2

# 1-20 배열
순서가 있는 자료형

# 2-1

# 2-2
단락평가

and, or 연산에서 앞의 값만 가지고 결과가 나온다면
뒤의 값은 보지도 않는다
함수를 사용하니 말로만 설명이 되는 과정을 눈으로 볼 수 있구나!

T || T => 앞에꺼
T && T => 뒤에꺼

# 2-3
구조분해할당

# 2-4
rest 매개변수 뒤에는 새로운 객체 선언 불가

# 2-5
원시 타입, 객체 타입
객체는 값 대신 참조값이 있음

객체 타입을 다룰 때, 의도치 않은 값 수정이 있을 수 있다.
객체 타입 다룰 땐, spread 연산자 같은걸로 긁어오면 된다.

얕복, 깊복
원본 객체가 수정되지 않게 조심하자!

객체간의 비교는 기본적으로 참조값 기준으로 이루어진다.
=== 는 참조값과 실제 프로퍼티 값 둘 다를 비교함

Array 와 fcuntion도 객체 타입이다!!

# 2-6
배열 순회, 객체 순회
언제쓰지
배열 of
객체 in

# 2-8

# 2-11 동기 비동기
비동기 차렷!!!
이제야 깨달

# 2-12 promise

# 2-14
async는 변환해주는게 맞았구나..!

# 3-1
노드 제이에스는 자바스크립트를 웹 브라우저 밖에서도 돌아갈 수
있도록 도와준다 -> 웹 서버, 모바일 앱, 데스크톱 앱

# 3-3
npm init -> 패키지 생성
node 파일명 -> 경로 잘 맞춰줘야함
package scripts start 설정해주면 npm run start 가능

# 3-4
모듈 -> 기능별로 나눠둔 것

# 3-5
미리 만들어진 모듈 시스템 - 라이브러리

# 4-3
모든 프로그램에 고유한 포트번호가 부여됨
5173 -> 로컬임

# 6-2
const 대문자

# 8-1
라이프 사이클
생애주기 ㅠㅠ
마운트 업데이트(리렌더링) 언마운트(컴포넌트의 죽음,..)

# 8-2
과식을 하면 살이 찐다 어쩌라고